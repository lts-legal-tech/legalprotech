import ConfigAccountWorkspace from '../../components/config-account-workspace';

export const metadata = {
  title: 'Config Account | LegalProTech',
};

export default function ConfigAccountPage() {
  return <ConfigAccountWorkspace />;
}
